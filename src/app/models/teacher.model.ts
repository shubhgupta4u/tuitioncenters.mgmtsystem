export class Teacher {
    id: number;
    firstName: string;
    lastName: string;
    batches:Array<string>;
    subjects:Array<string>;
    salary:string;
    startDate:string;    
    mobile: string;
    email: string;
    address:string;
}